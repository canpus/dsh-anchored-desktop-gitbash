{
  "name": "deepseek-harness-desktop",
  "version": "0.4.0",
  "private": true,
  "description": "Desktop shell for deepseek-harness — green package: thin shell over official dsh web (3080 child process) + anchored/router presets + titlebar/tray/updater",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "fetch-vendor": "node fetch-vendor.cjs"
  },
  "devDependencies": {
    "electron": "^43.4.0"
  }
}

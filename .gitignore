# ---- 上游与外部素材（不 fork 不重推）----
repo/
third-party/

# ---- 构建与打包产物 ----
dist/
desktop/node_modules/
desktop/vendor/
node.exe
desktop/node.exe

# ---- 凭据与个人素材（永不入库）----
DeepSeek API.txt
MIMO API.txt
AGENTS/
/AGENTS.md
*实测*.md

# ---- 台账（不入 Git：公开仓库 + 决策路径保密 + "永不回改"与脱敏冲突；防丢 = 本地时间戳备份 台账备份/；红线：不得进 zip 包——staging 只取 repo/ + desktop/，项目根天然排除）----
/History_log.md
/Current_State.md
/台账备份/

# ---- 阶段实施计划（plan_v*.md 不入 Git；根目录保留最近 3 版，更早移入 计划/；同样不进 zip 包）----
plan_v*.md
计划/

# ---- 日志 ----
*.log
docs/*.log

# ---- 壳运行时状态（缩放级别等，不入库）----
desktop/shell-state.json

# ---- 升级失败报告（本机运行产物）----
upgrade-report-*.md

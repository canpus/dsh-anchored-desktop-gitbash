# ---- 上游与外部素材（不 fork 不重推）----
repo/
third-party/

# ---- 构建与打包产物 ----
dist/
desktop/node_modules/
node.exe
desktop/node.exe

# ---- 凭据与个人素材（永不入库）----
DeepSeek API.txt
MIMO API.txt
AGENTS/
/AGENTS.md
*实测*.md

# ---- 台账（两本账随库管理；红线：不得进 zip 包——staging 只取 repo/ + desktop/，项目根天然排除）----

# ---- 日志 ----
*.log
docs/*.log

# ---- 壳运行时状态（缩放级别等，不入库）----
desktop/shell-state.json

# ---- 升级失败报告（本机运行产物）----
upgrade-report-*.md
